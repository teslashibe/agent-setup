apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: claude-agent-go

resources:
  - namespace.yaml
  - secret.example.yaml
  - migrate-job.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: ghcr.io/teslashibe/claude-agent-go
    newTag: latest
